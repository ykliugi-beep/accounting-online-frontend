# Local development environment
VITE_API_BASE_URL=http://localhost:5286/api/v1
VITE_ENABLE_MOCK_DATA=false

# JWT Token for testing (expires in 24h, regenerate daily from backend)
# Generate token by calling backend auth endpoint or use Swagger
VITE_JWT_TOKEN=your-test-token-here
